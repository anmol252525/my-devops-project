name: My first devops pipelines

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: checkout my code
        uses: actions/checkout@v3

      - name: print a message
        run: echo "pipeline is running"

      - name: list files in project
        run: ls -la



